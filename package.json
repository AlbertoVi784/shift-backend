{
  "name": "shift-pdf-processor",
  "version": "3.1.0",
  "description": "Cloud Run service — Estrazione turni da PDF/immagini via Gemini AI",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "deploy": "gcloud run deploy shift-pdf-processor --source . --region europe-west1 --allow-unauthenticated",
    "logs": "gcloud run services logs tail shift-pdf-processor --region europe-west1"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "express": "^4.21.2",
    "firebase-admin": "^13.0.2",
    "multer": "^1.4.5-lts.1"
  }
}
